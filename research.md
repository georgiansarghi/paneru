# Research: Upstream Paneru issues plausibly addressed by local diff

## Summary
I could not verify upstream GitHub issue numbers because the available toolset for this task did not include `web_search`, browser, GitHub API, or shell/network access. Based on the local diff summary and local documentation, the changes plausibly address upstream requests around scriptable/status-bar integration: direct window focusing, richer `subscribe` events for virtual workspaces, and native macOS tab-group reporting.

## Findings
1. **Issue numbers could not be verified in this environment** — The task requires checking `https://github.com/karinushka/paneru/issues`, but only local file read/write tools are available. I therefore cannot honestly name issue numbers without risking fabrication. [Upstream issues index](https://github.com/karinushka/paneru/issues)
2. **Direct focus commands plausibly address a “scriptable window focus / focus by index or id” issue** — Local docs now expose `window focus <index>`, `window focusid <id>`, `window_focus_<number>`, and `window_focusid_<window-id>`, which are specifically useful for scripts and status bars that render a window list and need to focus a selected item. Confidence in feature-to-issue fit: high if an upstream issue mentions focusing windows by number/id; confidence in issue number: none/blocked. Suggested branch: `issue-<number>-scriptable-window-focus`. [README.md](README.md), [CONFIGURATION.md](CONFIGURATION.md)
3. **Richer `windows_changed` events plausibly address a “SketchyBar/status bar stays stale” issue** — Local docs state that `subscribe --json` emits `windows_changed` with `virtual_workspaces`, and that events are emitted after creation/destruction/minimize/deminimize plus reorder, stack, manage, move/send between virtual workspaces, and other structural changes. This directly fits an issue asking integrations to update immediately without re-querying. Confidence in feature-to-issue fit: high if an issue mentions SketchyBar/window tabs/state subscription not updating; confidence in issue number: none/blocked. Suggested branch: `issue-<number>-sketchybar-windows-changed-events`. [QUERY_AND_SUBSCRIBE_FORMAT.md](QUERY_AND_SUBSCRIBE_FORMAT.md), [README.md](README.md)
4. **Virtual workspace query tab filtering plausibly addresses a “native macOS tabs show duplicate/non-front tabs” issue** — Local docs specify that native macOS tab groups are represented by their focused/front tab only in `virtual_workspaces`, which fits a status-bar/window-tabs rendering bug where hidden tabs should not appear as separate visible windows. Confidence in feature-to-issue fit: medium-high if an upstream issue mentions Ghostty/native macOS tabs/window tab lists; confidence in issue number: none/blocked. Suggested branch: `issue-<number>-native-tabs-front-tab-query`. [QUERY_AND_SUBSCRIBE_FORMAT.md](QUERY_AND_SUBSCRIBE_FORMAT.md), [README.md](README.md)
5. **The local branch name suggests a combined integration theme** — The checked-out local branch is named `sketchybar-window-tabs`, so the likely upstream target is an issue or issue cluster about SketchyBar/window-tab integration rather than an isolated internal refactor. Confidence: medium from local Git metadata only. Suggested combined branch if upstream maintainers prefer one PR: `issue-<number>-sketchybar-window-tabs`. [Local git reflog: `.git/logs/HEAD`](.git/logs/HEAD)

## Sources
- Kept: README.md (`/Users/georgiansarghi/paneru/README.md`) — Documents the new command surface, querying/subscribing model, and native macOS tabs support.
- Kept: CONFIGURATION.md (`/Users/georgiansarghi/paneru/CONFIGURATION.md`) — Documents the new binding names for numeric focus and focus-by-id.
- Kept: QUERY_AND_SUBSCRIBE_FORMAT.md (`/Users/georgiansarghi/paneru/QUERY_AND_SUBSCRIBE_FORMAT.md`) — Documents `windows_changed` payloads, virtual workspace snapshots, and front-tab-only tab-group behavior.
- Kept: local Git metadata (`/Users/georgiansarghi/paneru/.git/logs/HEAD`) — Shows the local feature branch `sketchybar-window-tabs` and commit summary.
- Dropped: Upstream GitHub issue pages — Could not be fetched with the available tools, so no issue titles/numbers were verified.

## Gaps
- Missing: verified upstream issue numbers, issue titles, author comments, labels, and open/closed state.
- Suggested next steps: run GitHub issue searches for `repo:karinushka/paneru sketchybar`, `repo:karinushka/paneru window tabs`, `repo:karinushka/paneru subscribe windows_changed`, `repo:karinushka/paneru focusid`, `repo:karinushka/paneru focus index`, and `repo:karinushka/paneru native tabs Ghostty`; then replace `issue-<number>-...` branch templates with exact issue-numbered names.
